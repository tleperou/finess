---
title: Custom props
description: A guide in my new Starlight docs site.
editUrl: https://urltoedit.com
tableOfContents:
  minHeadingLevel: 2
  maxHeadingLevel: 2
---

## Borders

import BorderWidth from './../../../../starlight/fragments/custom-props/borders/borders.widths.astro'
import BorderRadius from './../../../../starlight/fragments/custom-props/borders/borders.radius.astro'

### `border-width`

<BorderWidth>
  <p slot="block">The `border-block-width` CSS property defines the width of the logical block borders of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="inline">The `border-inline-width` CSS property defines the width of the logical inline borders of an element, which maps to a physical border width depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="shorthand">The `border-width` [shorthand](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) CSS property sets the width of an element's border.</p>
</BorderWidth>

#### Usage

```css
code {
  border-width: var(--border-width-1);
}

/* or within shortand */
code {
  border: var(--border-width-1) solid var(--color-neutral-1);
}
```

### `border-radius`

<BorderRadius>
  <p slot="shorthand">The `border-radius` CSS property rounds the corners of an element's outer border edge. You can set a single radius to make circular corners, or two radii to make elliptical corners.</p>
</BorderRadius>

#### Usage

```css
code {
  border-radius: var(--border-radius-1);
}
```

## Fonts

Font properties priotize the preferences of the user and its operating system. They vary according the size of the viewport in the respect of the [WCAG SC 1.4.4](https://www.w3.org/WAI/WCAG21/Understanding/resize-text.html) accessibility rules.

import FontFamilies from './../../../../starlight/fragments/custom-props/fonts/fonts.font-family.astro'
import FontSizes from './../../../../starlight/fragments/custom-props/fonts/fonts.font-size.astro'
import FontWeight from './../../../../starlight/fragments/custom-props/fonts/fonts.font-weight.astro'
import LetterSpacing from './../../../../starlight/fragments/custom-props/fonts/fonts.letter-spacing.astro'
import LineHeight from './../../../../starlight/fragments/custom-props/fonts/fonts.line-height.astro'

### `font-family`

The `font-family` CSS property specifies a prioritized list of one or more font family names and/or generic family names for the selected element.

<FontFamilies />

#### Usage

```css
code {
  font-family: var(--font-mono);
}
```

### `font-size`

The `font-size` CSS property sets the size of the font relatively to the dimensions of the viewport.

<FontSizes />

#### Usage

```css
code {
  font-size: var(--font-size-1);
}
```

### `font-weight`

The `font-weight` CSS property sets the weight (or boldness) of the font. The weights available depend on the [`font-family`](https://developer.mozilla.org/en-US/docs/Web/CSS/font-family) that is currently set.

<FontWeight />

#### Usage

```css
code {
  font-weight: var(--font-weight-1);
}
```

### `letter-spacing`

The `letter-spacing` CSS property sets the horizontal spacing behavior between text characters.

<LetterSpacing />

#### Usage

```css
code {
  letter-spacing: var(--letter-spacing-1);
}
```


### `line-height`

The `line-height` CSS property sets the height of a line box. It's commonly used to set the distance between lines of text. On block-level elements, it specifies the minimum height of line boxes within the element.

<LineHeight />

#### Usage

```css
code {
  line-height: var(--line-height-1);
}
```

## Layers

import Layers from './../../../../starlight/fragments/custom-props/layers/layer.z-index.astro'

### `z-index`

The `z-index` CSS property sets the z-order of a [positioned](https://developer.mozilla.org/en-US/docs/Web/CSS/position) element and its descendants or flex and grid items

<Layers />

## Spacings

import Gaps from './../../../../starlight/fragments/custom-props/spacings/spacings.gaps.astro'
import Margins from './../../../../starlight/fragments/custom-props/spacings/spacings.margins.astro'
import Paddings from './../../../../starlight/fragments/custom-props/spacings/spacings.paddings.astro'

Spacings are relative to the element's [`font-size`](https://developer.mozilla.org/en-US/docs/Web/CSS/font-size) and [`line-height`](https://developer.mozilla.org/en-US/docs/Web/CSS/line-height) based upon the average glyph's dimensions. It also vary with the height, width and orientation of the viewport.

### `gap`

<Gaps>
  <p slot="row">The [`row-gap`](https://developer.mozilla.org/en-US/docs/Web/CSS/row-gap) CSS property sets the size of the gap between an element's rows.</p>
  <p slot="column">The [`column-gap`](https://developer.mozilla.org/en-US/docs/Web/CSS/column-gap) CSS property sets the size of the gap between an element's columns.</p>
  <p slot="shorthand">The <a href="https://developer.mozilla.org/en-US/docs/Web/CSS/row-gap"><code>row-gap</code></a> CSS property sets the size of the gap between an element's rows.</p>
</Gaps>

### `margin`

<Margins>
  <p slot="block">The margin-block CSS [shorthand property](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) defines the logical block start and end margins of an element, which maps to physical margins depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="inline">The margin-inline CSS [shorthand property](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) is a shorthand property that defines both the logical inline start and end margins of an element, which maps to physical margins depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="shorthand">The margin CSS shorthand property sets the [margin area](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_model/Introduction_to_the_CSS_box_model#margin_area) on all four sides of an element.</p>
</Margins>

### `padding`

<Paddings>
  <p slot="inline">The padding-inline CSS [shorthand property](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) defines the logical inline start and end padding of an element, which maps to physical padding properties depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="block">The padding-block CSS [shorthand property](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) defines the logical block start and end padding of an element, which maps to physical padding properties depending on the element's writing mode, directionality, and text orientation.</p>
  <p slot="shorthand">The padding CSS shorthand property sets the [shorthand property](https://developer.mozilla.org/en-US/docs/Web/CSS/Shorthand_properties) padding area on all four sides of an element at once. between an element's rows and columns.</p>
</Paddings>
