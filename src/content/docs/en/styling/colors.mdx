---
title: Colors
tableOfContents:
  minHeadingLevel: 1
  maxHeadingLevel: 2
---

import Colors from './../../../../starlight/fragments/colors/colors.astro'

import ColorSurface from './../../../../starlight/fragments/colors/colors.surface.astro'
import ColorAccent from './../../../../starlight/fragments/colors/colors.accent.astro'
import ColorNegative from './../../../../starlight/fragments/colors/colors.negative.astro'
import ColorNeutral from './../../../../starlight/fragments/colors/colors.neutral.astro'
import ColorPositive from './../../../../starlight/fragments/colors/colors.positive.astro'

import PaletteSurface from './../../../../starlight/fragments/colors/palette.surface.astro'
import PaletteAccent from './../../../../starlight/fragments/colors/palette.accent.astro'
import PaletteNegative from './../../../../starlight/fragments/colors/palette.negative.astro'
import PaletteNeutral from './../../../../starlight/fragments/colors/palette.neutral.astro'
import PalettePositive from './../../../../starlight/fragments/colors/palette.positive.astro'

import ColorDevice from './../../../../starlight/fragments/colors/colors.device.astro'
import ColorPicker from './../../../../starlight/fragments/colors/colors.picker.astro'
import ContrastPicker from './../../../../starlight/fragments/colors/contrast.picker.astro'

Finess defines colors upon the [`<system-color>`](https://developer.mozilla.org/en-US/docs/Web/CSS/system-color#mark) of the device with the users' contrast preferences using [`color-mix`](https://developer.mozilla.org/en-US/docs/Web/CSS/color_value/color-mix) as possible.

<aside>Personalise the palettes using the color and constrast pickers.</aside>

### Usage

```css "var(--color-on-surface-1)" "var(--color-on-surface-2)" "var(--color-surface-1)"
hgroup {
  display: flex;
  flex-direction: column;
  row-gap: var(--row-gap-1);
  background-color: var(--color-surface-1);

  & h1 {
    color: var(--color-on-surface-1);
  }

  & small {
    color: var(--color-on-surface-2);
  }
}
```

<Colors>

  ## Surface

  The surface color uses the [`Canvas`](https://developer.mozilla.org/en-US/docs/Web/CSS/system-color#mark) when supported that is generarly used as <strong>main background and text</strong> colors.

  <ColorSurface />

  ### Palette
  
  <PaletteSurface />

  ## Accent

  The accent colors use the [`AccentColor`](https://developer.mozilla.org/en-US/docs/Web/CSS/system-color#mark) when supported that can be used for your <strong>branding color</strong> for example.

  <aside>Change the accent color of your system to see the accent palette in action.</aside>

  <ColorAccent />

  ### Palette

  <PaletteAccent />

  ## Negative

  The negative colors use predefined values that can be used with <strong>displaying errors</strong> for instance.

  <ColorNegative />

  ### Palette

  <PaletteNegative />

  ## Neutral

  The neutral colors use predefined values that can be used with <strong>showing aside piece of information</strong> for instance.

  <ColorNeutral />

  ### Palette

  <PaletteNeutral />

  ## Positive

  The positive colors use predefined values that can be used with <strong>successful interaction</strong>'s output for instance.

  <ColorPositive />

  ### Palette

  <PalettePositive />

</Colors>

## Color scheme

Finess color palettes come with dark and light color schemes in combinaison with `less` and `more` user's preference contrast levels.

In the event you want to override the default user's color scheme preference, use `data-prefers-color-scheme` onto the `html` tag – even though overriding users preferences shall be avoided.

<aside>
  <p>Color scheme can also be locally overidden, read more about [`color-scheme`](https://developer.mozilla.org/en-US/docs/Web/CSS/color-scheme).</p>
</aside>

### Overriding with HTML

When using the overriden color scheme preference:

```css
html[data-prefers-color-scheme='dark'] .my-component {
  --color: indianred;
}

html[data-prefers-color-scheme='light'] .my-component {
  --color: darkred;
}
```

### Overriding with CSS

```css
html[data-prefers-color-scheme='dark']  {
  color-scheme: dark only;
}

html[data-prefers-color-scheme='light'] {
  color-scheme: light only;
}
```

### Targeting color scheme

When using the [User Agent](https://developer.mozilla.org/en-US/docs/Glossary/User_agent) color scheme preference:

```css
/* dark by default */
.my-component {
  --color: indianred;

  @media (--light) {
      --color: darkred;
  }
}

/* light by default */
.my-component {
  --color: darkred;

  @media (--dark) {
      --color: indianred;
  }
}

/* or no defaults */
.my-component {
  @media (--light) {
      --color: darkred;
  }

  @media (--dark) {
      --color: indianred;
  }
}
```
